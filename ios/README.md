# iOS Capacitor wrapper

This directory is a placeholder until native projects are generated.

## Generate and sync

```bash
npx cap add ios
CAPACITOR_BUILD=true npm run build && npx cap sync
```

## Open in Xcode

```bash
open ios/App/App.xcworkspace
```

Or open `ios/App/App.xcworkspace` manually from Xcode.
