import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.kepi.travelassistant",
  appName: "Kepi Travel Assistant",
  webDir: "out",
};

export default config;
