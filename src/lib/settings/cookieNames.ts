/** HttpOnly session for /settings (signed, short-lived). */
export const COOKIE_SETTINGS_SESSION = "kepi_settings_session";

/** HttpOnly AES-GCM encrypted JSON (IntegrationsVaultV1). */
export const COOKIE_INTEGRATIONS_VAULT = "kepi_integrations_v1";
